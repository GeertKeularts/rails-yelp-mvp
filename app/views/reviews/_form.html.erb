<%= form_for [@restaurant, @review] do |f| %>
<%= f.label :content %>
<%= f.text_field :content %>
<br>
<%= f.label :rating %>
<%= f.select :rating, options_for_select( [0, 1, 2, 3, 4, 5])  %>
<br>

<br>
<br>
<span class="button"><%= f.submit "add a review"%></span>

<% end %>